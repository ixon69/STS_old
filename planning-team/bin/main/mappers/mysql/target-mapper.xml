<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TargetMapper">
	<insert id="insertPhoneInside" parameterType="java.util.HashMap">
		/* target-mapper.xml | insertPhoneInside */
		INSERT INTO SPRING_BATCH.PHONE_INSIDE (
			ID
			,HEADQUARTER
			,DEPARTMENT
			,PART
			,PHONE
		)
		VALUES
		(
			#{ID}
			,#{HEADQUARTER}
			,#{DEPARTMENT}
			,#{PART}
			,#{PHONE}
		)
	</insert>
</mapper>
