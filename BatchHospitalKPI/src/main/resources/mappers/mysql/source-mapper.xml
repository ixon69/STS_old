<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="SourceMapper">
    <select id="selectPhoneInside" parameterType="String" resultType="java.util.HashMap">
    	/* source-mapper.xml | selectPhoneInside */
		SELECT
			ID
			,HEADQUARTER
			,DEPARTMENT
			,PART
			,PHONE
		FROM
			PHONE_INSIDE
		WHERE
			PART = #{part}
	</select>
</mapper>
